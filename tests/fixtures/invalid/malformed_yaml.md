---
relation:root: [unclosed flow sequence
title: "unterminated string
---

The frontmatter block above is not valid YAML: an unclosed flow sequence and an unterminated
quoted string. The parser must report malformed YAML rather than panicking.
