---
title: Summary as a nested mapping
relation:root: 01a03d5a-6f7a-7b8c-9d0e-2f3a4b5c6d7e
summary:
  short:  one line
  long:   several, with a trailing comment   # kept, because it is inside the key's span
  nested:
    - deeper
    - still deeper
---

The other half of the unknown-key guarantee. `summary` here is a nested mapping with hand-aligned
values, a comment, and a sequence under a third level. An emitter would re-space the alignment and
drop the comment; slicing the key's source lines keeps both.
